#include <iostream>
#include <vector>
#include "main.h"

//add function

int main(){

    //INVERT
    myuint<4>M(1 );      //<4> - No of bits || <5> Decimal to be converted
    myuint<4>T(5 );

    myuint<4>A  = M + T;

    // cout << (A);
    //myuint<4>M("2"); 
    
    //?Checker to see if number given is too big for bits?
}